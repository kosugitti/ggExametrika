# exametrika v1.9.0 プロットタイプ分析

## 📊 全体概要

**exametrika v1.9.0 総プロットタイプ数: 28**

- ✅ 実装済み: 21 (75%)
- ❌ 未実装: 7 (25%)

## 🆕 v1.9.0 新規追加プロット（多値バイクラスタリング専用）

### 1. FCRP - Field Category Response Profile
- **対応モデル**: nominalBiclustering, ordinalBiclustering
- **機能**: 各フィールドのカテゴリ確率プロット
- **パラメータ**: `style` (line/bar)
- **類似**: ICRP のフィールド版

### 2. FCBR - Field Cumulative Boundary Response
- **対応モデル**: ordinalBiclustering（ordinal専用）
- **機能**: 各フィールドの境界確率プロット
- **類似**: ICBR のフィールド版

### 3. ScoreField
- **対応モデル**: nominalBiclustering, ordinalBiclustering
- **機能**: 期待得点ヒートマップ（フィールド × クラス/ランク）
- **類似**: ScoreRank のバイクラスタリング版

### 4. FRP（多値版）
- **対応モデル**: nominalBiclustering, ordinalBiclustering
- **機能**: 期待得点折れ線プロット
- **パラメータ**: `stat` (mean/median/mode)
- **既存との違い**: 既存のplotFRP_ggは二値版、多値版はstatパラメータが必要

### 5. RRV（多値版）
- **対応モデル**: nominalBiclustering, ordinalBiclustering
- **機能**: ランク参照ベクトル
- **パラメータ**: `stat` (mean/median/mode)
- **既存との違い**: 既存のplotRRV_ggはBiclustering用、多値版は別途実装が必要

## 📋 既存未実装プロット

### 6. ScoreRank
- **対応モデル**: LRAordinal, LRArated
- **状況**: **他メンバー担当中**
- **機能**: スコア-ランクヒートマップ

### 7. LDPSR
- **対応モデル**: BINET
- **機能**: Latent Dependence Passing Student Rate

## 🎯 実装優先度の提案

### 優先度1: FCRP, FCBR, ScoreField（多値バイクラスタリング基本プロット）
- exametrika v1.9.0の主要新機能
- 多値データ分析に不可欠

### 優先度2: FRP（多値版）, RRV（多値版）
- 既存関数の拡張
- statパラメータ対応が必要

### 優先度3: LDPSR
- BINET専用
- 使用頻度は低い可能性

## 📝 技術的ポイント

### FCRP実装時の注意点
```r
# exametrika実装参照: R/00_plot_poly_biclustering.R
# - style = "line": 折れ線プロット
# - style = "bar": 積み上げ棒グラフ
# データ構造: x$FRP (3次元配列: field × class × category)
```

### FCBR実装時の注意点
```r
# ordinalBiclustering専用（nominalには適用不可）
# 累積確率計算: P(Q >= k) = Σ P(Q = j) for j >= k
# ICBRと同じロジック、フィールド単位で処理
```

### ScoreField実装時の注意点
```r
# ヒートマップ表示
# 期待得点: Σ(k * P(Q = k))
# geom_tile()でセル描画、geom_text()で数値表示
```

## 🔗 参考ファイル

- exametrika本体: `c:/Users/hp221/exametrika-dev/R/00_plot_poly_biclustering.R`
- valid_types定義: `c:/Users/hp221/exametrika-dev/R/00_exametrikaPlot.R` (172-186行)
