% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IRPtoCMPRMP.R
\name{.plot_student_distribution}
\alias{.plot_student_distribution}
\title{Shared worker for plotLCD_gg / plotLRD_gg}
\usage{
.plot_student_distribution(
  data,
  Num_Students,
  title,
  colors,
  linetype,
  show_legend,
  legend_position,
  prefer
)
}
\arguments{
\item{data}{An object of class \code{c("exametrika", "LCA")} or
\code{c("exametrika", "BINET")}. If LRA or Biclustering output
is provided, LRD will be plotted instead with a warning.}

\item{Num_Students}{Logical. If \code{TRUE} (default), display the
number of students on each bar.}

\item{title}{Logical or character. If \code{TRUE} (default), display the
auto-generated title. If \code{FALSE}, no title. If a character string,
use it as a custom title.}

\item{colors}{Character vector of length 2. First element is the bar fill
color, second is the line/point color. If \code{NULL} (default), uses
gray for bars and black for line/points.}

\item{linetype}{Character or numeric specifying the line type for the
frequency line. Default is \code{"dashed"}.}

\item{show_legend}{Logical. If \code{TRUE}, display the legend.
Default is \code{FALSE}.}

\item{legend_position}{Character. Position of the legend.
One of \code{"right"} (default), \code{"top"}, \code{"bottom"},
\code{"left"}, \code{"none"}.}

\item{prefer}{Either "class" (plotLCD_gg) or "rank" (plotLRD_gg).}
}
\value{
A ggplot object.
}
\description{
The two exported functions draw the same student-distribution plot;
they differ only in which model family is the "native" one (class vs
rank) and therefore in which direction the redirect warning fires.
}
\keyword{internal}
