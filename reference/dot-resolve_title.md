# Resolve the title argument (logical or character)

Resolve the title argument (logical or character)

## Usage

``` r
.resolve_title(title, auto_title)
```

## Arguments

- title:

  TRUE (auto title), FALSE (no title), or a character string.

- auto_title:

  The automatic title used when `title` is TRUE.

## Value

A character title or NULL.
